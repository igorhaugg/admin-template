import React from 'react';

import Header from '../header/Header';
import Hero from '../hero/Hero';

const Home = () => {
  return (
    <main>
      <Header />
      <Hero />
    </main>
  );
};

export default Home;
